class DivisionSummary {
    constructor(address, email, fingerprints, journal, phone){
        this.address = address;
        this.email = email;
        this.fingerprints = fingerprints;
        this.journal = journal;
        this.phone = phone;
    }
}

export default DivisionSummary;